# 🍩 Hoshino-AI
 A **Discord Bot AI** inspired and was taken from the personality of a Blue Archive character which is Takanashi Hoshino. 

# 🧿 Core Features
Internalized AI personality, the Bot is well-enough literate about the character roleplay. 

Uses the`/` commands function for easier access. 

Can also use `h!` for a more direct command. 

# 🤖 Commands
These are the commands of how to use Hoshino.  To further more have an idea of her specific commands. Just use `/help` 
or `h!help`.

`/` or `h!greet`

`/` or `h!joke`

`/` or `h!sendemoji [text]`

`/` or `h!chat [message]`

`/` or `h!hug`

`/` or `h!sleep`

`/` or `h!fishfact`

`/` or `h!compliment`

`/` or `h!schedule`

`/` or `h!treasure`

`/` or `h!whale`

`/` or `h!map`

`/` or `h!lullaby`

`/` or `h!story`
